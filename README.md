# Kubernetes Go API

This is a simple Go program that demonstrates how to interact with the Kubernetes API to manage deployments.

## Development

To run the program, you will need to have a Kubernetes cluster and a kubeconfig file.

```bash
go run main.go -kubeconfig=/path/to/your/kubeconfig
```
